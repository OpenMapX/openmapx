diff --git a/dist/types/heif.js b/dist/types/heif.js
index 7997d3f159d60da91f73b498e59714298e0effd1..a113b8a3a4dfb830a4315d8a78f68aed851192fa 100644
--- a/dist/types/heif.js
+++ b/dist/types/heif.js
@@ -24,10 +24,13 @@ exports.HEIF = {
     },
     calculate(input) {
         // Based on https://nokiatech.github.io/heif/technical.html
-        const metaBox = (0, utils_1.findBox)(input, 'meta', 0);
-        const iprpBox = metaBox && (0, utils_1.findBox)(input, 'iprp', metaBox.offset + 12);
-        const ipcoBox = iprpBox && (0, utils_1.findBox)(input, 'ipco', iprpBox.offset + 8);
-        const ispeBox = ipcoBox && (0, utils_1.findBox)(input, 'ispe', ipcoBox.offset + 8);
+        const metaBox = (0, utils_1.findBox)(input, 'meta', 0, input.length);
+        const metaEnd = metaBox && metaBox.offset + metaBox.size;
+        const iprpBox = metaBox && (0, utils_1.findBox)(input, 'iprp', metaBox.offset + 12, metaEnd);
+        const iprpEnd = iprpBox && iprpBox.offset + iprpBox.size;
+        const ipcoBox = iprpBox && (0, utils_1.findBox)(input, 'ipco', iprpBox.offset + 8, iprpEnd);
+        const ipcoEnd = ipcoBox && ipcoBox.offset + ipcoBox.size;
+        const ispeBox = ipcoBox && (0, utils_1.findBox)(input, 'ispe', ipcoBox.offset + 8, ipcoEnd);
         if (ispeBox) {
             return {
                 height: (0, utils_1.readUInt32BE)(input, ispeBox.offset + 16),
diff --git a/dist/types/icns.js b/dist/types/icns.js
index f2bfafef3723cb423b110304815e56e3f97f81e0..97366799aeee4fdf46f3d73d36454900acffe5b4 100644
--- a/dist/types/icns.js
+++ b/dist/types/icns.js
@@ -78,24 +78,35 @@ exports.ICNS = {
     validate: (input) => (0, utils_1.toUTF8String)(input, 0, 4) === 'icns',
     calculate(input) {
         const inputLength = input.length;
+        if (inputLength < SIZE_HEADER)
+            throw new TypeError('Invalid ICNS, truncated header');
         const fileLength = (0, utils_1.readUInt32BE)(input, FILE_LENGTH_OFFSET);
+        if (fileLength < SIZE_HEADER || fileLength > inputLength)
+            throw new TypeError('Invalid ICNS, invalid file length');
         let imageOffset = SIZE_HEADER;
-        let imageHeader = readImageHeader(input, imageOffset);
-        let imageSize = getImageSize(imageHeader[0]);
-        imageOffset += imageHeader[1];
-        if (imageOffset === fileLength)
-            return imageSize;
-        const result = {
-            height: imageSize.height,
-            images: [imageSize],
-            width: imageSize.width,
-        };
-        while (imageOffset < fileLength && imageOffset < inputLength) {
-            imageHeader = readImageHeader(input, imageOffset);
-            imageSize = getImageSize(imageHeader[0]);
-            imageOffset += imageHeader[1];
-            result.images.push(imageSize);
+        const images = [];
+        while (imageOffset < fileLength) {
+            const remaining = Math.min(fileLength, inputLength) - imageOffset;
+            if (remaining < SIZE_HEADER)
+                throw new TypeError('Invalid ICNS, truncated entry header');
+            const imageHeader = readImageHeader(input, imageOffset);
+            const imageLength = imageHeader[1];
+            if (imageLength < SIZE_HEADER || imageLength > remaining)
+                throw new TypeError('Invalid ICNS, invalid entry length');
+            const nextOffset = imageOffset + imageLength;
+            if (!Number.isSafeInteger(nextOffset) || nextOffset <= imageOffset)
+                throw new TypeError('Invalid ICNS, non-advancing entry');
+            images.push(getImageSize(imageHeader[0]));
+            imageOffset = nextOffset;
         }
-        return result;
+        if (images.length === 0)
+            throw new TypeError('Invalid ICNS, no sizes found');
+        if (images.length === 1)
+            return images[0];
+        return {
+            height: images[0].height,
+            images,
+            width: images[0].width,
+        };
     },
 };
diff --git a/dist/types/jp2.js b/dist/types/jp2.js
index 8af3f77cb51aabc112203ed6cf2d5339caaf65a7..09dd4885e8191b837271821b48a56547b76acfaf 100644
--- a/dist/types/jp2.js
+++ b/dist/types/jp2.js
@@ -14,8 +14,9 @@ exports.JP2 = {
         return brand === 'jp2 ';
     },
     calculate(input) {
-        const jp2hBox = (0, utils_1.findBox)(input, 'jp2h', 0);
-        const ihdrBox = jp2hBox && (0, utils_1.findBox)(input, 'ihdr', jp2hBox.offset + 8);
+        const jp2hBox = (0, utils_1.findBox)(input, 'jp2h', 0, input.length);
+        const jp2hEnd = jp2hBox && jp2hBox.offset + jp2hBox.size;
+        const ihdrBox = jp2hBox && (0, utils_1.findBox)(input, 'ihdr', jp2hBox.offset + 8, jp2hEnd);
         if (ihdrBox) {
             return {
                 height: (0, utils_1.readUInt32BE)(input, ihdrBox.offset + 8),
diff --git a/dist/types/jxl.js b/dist/types/jxl.js
index f557b0aea7e679d2cb84d9915e957dd2254eba97..f40e5c5876c371ebccbc8633f5a8d8b994e80d5c 100644
--- a/dist/types/jxl.js
+++ b/dist/types/jxl.js
@@ -5,7 +5,7 @@ const utils_1 = require("./utils");
 const jxl_stream_1 = require("./jxl-stream");
 /** Extracts the codestream from a containerized JPEG XL image */
 function extractCodestream(input) {
-    const jxlcBox = (0, utils_1.findBox)(input, 'jxlc', 0);
+    const jxlcBox = (0, utils_1.findBox)(input, 'jxlc', 0, input.length);
     if (jxlcBox) {
         return input.slice(jxlcBox.offset + 8, jxlcBox.offset + jxlcBox.size);
     }
@@ -20,11 +20,14 @@ function extractPartialStreams(input) {
     const partialStreams = [];
     let offset = 0;
     while (offset < input.length) {
-        const jxlpBox = (0, utils_1.findBox)(input, 'jxlp', offset);
+        const jxlpBox = (0, utils_1.findBox)(input, 'jxlp', offset, input.length);
         if (!jxlpBox)
             break;
         partialStreams.push(input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size));
-        offset = jxlpBox.offset + jxlpBox.size;
+        const nextOffset = jxlpBox.offset + jxlpBox.size;
+        if (nextOffset <= offset)
+            throw new TypeError('Invalid JXL, non-advancing box');
+        offset = nextOffset;
     }
     return partialStreams;
 }
diff --git a/dist/types/utils.d.ts b/dist/types/utils.d.ts
index 7c79ca8271489d4405bb98559dfe7f13fc50eade..f661de3c1e89990fd883e310fea2d6afb079ffd2 100644
--- a/dist/types/utils.d.ts
+++ b/dist/types/utils.d.ts
@@ -8,7 +8,7 @@ export declare const readInt32LE: (input: Uint8Array, offset?: number) => number
 export declare const readUInt32BE: (input: Uint8Array, offset?: number) => number;
 export declare const readUInt32LE: (input: Uint8Array, offset?: number) => number;
 export declare function readUInt(input: Uint8Array, bits: 16 | 32, offset: number, isBigEndian: boolean): number;
-export declare function findBox(input: Uint8Array, boxName: string, offset: number): {
+export declare function findBox(input: Uint8Array, boxName: string, offset: number, parentEnd?: number): {
     name: string;
     offset: number;
     size: number;
diff --git a/dist/types/utils.js b/dist/types/utils.js
index 5224bbafe87551ac415cb3de234820ccc0ff6e2c..ff5f7ccdb6ec360093543c3b8c730b0568e1198c 100644
--- a/dist/types/utils.js
+++ b/dist/types/utils.js
@@ -48,28 +48,35 @@ function readUInt(input, bits, offset, isBigEndian) {
     return methods[methodName](input, offset);
 }
 exports.readUInt = readUInt;
-function readBox(input, offset) {
-    if (input.length - offset < 4)
+function readBox(input, offset, parentEnd = input.length) {
+    const end = Math.min(parentEnd, input.length);
+    if (!Number.isSafeInteger(offset) || offset < 0 || end - offset < 8)
         return;
     const boxSize = (0, exports.readUInt32BE)(input, offset);
-    if (input.length - offset < boxSize)
+    if (boxSize === 1)
+        return;
+    const size = boxSize === 0 ? end - offset : boxSize;
+    const nextOffset = offset + size;
+    if (size < 8 || !Number.isSafeInteger(nextOffset) || nextOffset <= offset || nextOffset > end)
         return;
     return {
         name: (0, exports.toUTF8String)(input, 4 + offset, 8 + offset),
         offset,
-        size: boxSize,
+        size,
     };
 }
-function findBox(input, boxName, offset) {
-    while (offset < input.length) {
-        const box = readBox(input, offset);
+function findBox(input, boxName, offset, parentEnd = input.length) {
+    const end = Math.min(parentEnd, input.length);
+    while (offset < end) {
+        const box = readBox(input, offset, end);
         if (!box)
             break;
         if (box.name === boxName)
             return box;
-        // Fix the infinite loop by ensuring offset always increases
-        // If box.size is 0, advance by at least 8 bytes (the size of the box header)
-        offset += box.size > 0 ? box.size : 8;
+        const nextOffset = box.offset + box.size;
+        if (nextOffset <= offset)
+            break;
+        offset = nextOffset;
     }
 }
 exports.findBox = findBox;
