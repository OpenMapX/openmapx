{
  "name": "Weather Overlay",
  "description": "Precipitation radar, temperature, and other weather map layers",
  "dataSources": {
    "rainviewer": {
      "purpose": "Precipitation radar animation (metadata server-only, but radar TILES loaded directly by browser)",
      "dataSent": "No user data (static metadata feed)",
      "dataReceived": "CDN host URL, past radar frames and nowcast predictions; PNG radar tiles"
    },
    "openweathermap": {
      "purpose": "Weather parameter overlay tiles (temperature, clouds, wind, pressure, precipitation)",
      "dataSent": "Weather layer name, tile coordinates (z/x/y)",
      "dataReceived": "PNG tiles showing the selected weather parameter"
    }
  }
}
