{
  "id": "routing-valhalla",
  "version": "1.0.0",
  "author": "OpenMapX",
  "license": "MIT",
  "domains": ["routing"],
  "dependencies": ["routing"],
  "infrastructure": {
    "dataRequirements": ["osm-pbf"]
  },
  "backend": {
    "routes": false
  },
  "configSchema": {
    "type": "object",
    "properties": {
      "enabled": {
        "type": "boolean",
        "default": true
      },
      "endpoint": {
        "type": "string",
        "title": "Valhalla endpoint URL",
        "description": "Base URL of the Valhalla server. Defaults to Stadia Maps (https://api.stadiamaps.com), which needs an API key below. Leave blank to use the default, point at a self-hosted service resolved via the `valhalla` capability, or set another Valhalla-compatible host.",
        "format": "url"
      },
      "apiKey": {
        "type": "string",
        "title": "Valhalla API key",
        "description": "API key appended as `?api_key=` to requests. Required for Stadia Maps (free non-commercial tier); leave blank for key-less self-hosted instances. Also settable via the VALHALLA_API_KEY env var.",
        "format": "password",
        "x-openmapx-setup": {
          "url": "https://client.stadiamaps.com/dashboard/",
          "urlLabel": "Get Stadia Maps API key",
          "steps": [
            "Sign up at client.stadiamaps.com/signup (free, no credit card).",
            "Open the dashboard and click 'Manage Properties'.",
            "Select your property from the dropdown.",
            "Under Authentication Configuration, generate your API key and copy it."
          ],
          "cost": "Free tier: 200,000 credits/month, no credit card.",
          "notes": "Only needed for the hosted Stadia Maps default; self-hosted Valhalla needs no key. Stadia's free tier is non-commercial / evaluation use only."
        }
      }
    }
  },
  "healthCheck": [
    {
      "name": "Self-hosted",
      "type": "ping",
      "urlTemplate": "${service:valhalla}/status"
    },
    {
      "name": "Stadia Maps",
      "type": "http",
      "urlTemplate": "https://api.stadiamaps.com/geocoding/v1/search?text=test&size=1&api_key=${apiKey}",
      "requiredConfigKeys": ["apiKey"]
    }
  ],
  "quality": "built-in",
  "category": "Routing",
  "dataSources": [
    {
      "sourceId": "valhalla",
      "name": "Valhalla (Stadia Maps)",
      "url": "https://stadiamaps.com/",
      "license": "MIT (Valhalla routing engine); Stadia Maps",
      "licenseUrl": "https://stadiamaps.com/terms-of-service/",
      "attribution": "Routing © <a href=\"https://stadiamaps.com/\">Stadia Maps</a>, © <a href=\"https://www.openstreetmap.org/copyright\">OpenStreetMap contributors</a>",
      "notes": "Default Valhalla host. Free tier requires an API key (VALHALLA_API_KEY) and is limited to non-commercial / evaluation use; commercial use needs a paid Stadia plan. A self-hosted or other Valhalla-compatible endpoint can be set via VALHALLA_URL (no key).",
      "commercialUse": "conditional",
      "providerCountry": "US",
      "providerPrivacyUrl": "https://stadiamaps.com/privacy/privacy-policy/",
      "endUserExposure": "server-only",
      "personalData": false,
      "cookies": false,
      "dpaAvailable": true,
      "dpaUrl": "https://stadiamaps.com/legal/data-processing-addendum/"
    }
  ],
  "requires": [
    {
      "service": "valhalla",
      "optional": true
    }
  ]
}
