"use client";

// Phase 6 — Satellite imagery layer via MapTiler
// Switches the map style to the satellite tileset when the satellite
// layer toggle is active in layerStore.

export function SatelliteLayer() {
  return null;
}
