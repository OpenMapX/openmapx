"use client";

// Phase 6 — Transit layer
// Overlays public transit routes from OpenTripPlanner GTFS feeds
// when showTransit is enabled in layerStore.

export function TransitLayer() {
  return null;
}
