/** Brand colour palette shared across all web UI components. */
export const TEAL = "#007b8b";
export const TEAL_LIGHT = "#e0f2f1";

/** Primary blue — MUI theme primary.main. Use in map layers that can't access useTheme(). */
export const PRIMARY_BLUE = "#1A73E8";
