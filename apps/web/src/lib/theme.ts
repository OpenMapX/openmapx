/** Brand colour palette. Uses CSS custom properties for automatic light/dark switching. */
export const TEAL = "var(--omx-teal)";
export const TEAL_LIGHT = "var(--omx-teal-light)";

/** Primary blue — matches MUI primary.main. Uses CSS variable for dark mode. */
export const PRIMARY_BLUE = "var(--omx-primary-blue)";

/** Raw hex values for contexts that don't support CSS variables (e.g. MapLibre paint). */
export const PRIMARY_BLUE_HEX = "#1A73E8";
export const TEAL_HEX = "#007b8b";
