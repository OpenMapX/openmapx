{
  "$comment": "The versions a release is built with. Recorded rather than merely required, because 'it built on my machine' is not a claim anybody can check six months later when a report arrives against this exact build.",
  "node": ">=24.0.0",
  "pnpm": "11.15.1",
  "expoSdk": "57",
  "reactNative": "0.86.2",
  "ios": {
    "xcode": ">=26.4",
    "deploymentTarget": "16.4",
    "$comment": "CocoaPods resolution is pinned by the committed Podfile.lock under native-locks/."
  },
  "android": {
    "$comment": "AGP's JdkImageTransform cannot use a JDK newer than 21, so the build refuses to run on one rather than failing deep inside Gradle with an unrelated-looking message.",
    "javaMajor": [17, 21],
    "minSdk": 24,
    "compileSdk": 36,
    "targetSdk": 36
  }
}
