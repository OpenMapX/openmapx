/**
 * Globe projection stub for React Native.
 *
 * MapLibre React Native v11 beta does not expose a `projection` prop on the
 * Map component and the native SDK does not yet support globe projection.
 * This component is a placeholder so the layer system can reference it
 * uniformly; it renders nothing.
 */
export function GlobeProjection() {
  return null;
}
