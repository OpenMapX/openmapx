---
"@openmapx/core": minor
---

Add the personal-vehicle contract, a local-or-account garage backend with shared
validation, and per-vehicle parked-location state with query hooks, a parking
store and a parking panel id. Replaces the single hand-entered EV vehicle slot in
the settings store.
