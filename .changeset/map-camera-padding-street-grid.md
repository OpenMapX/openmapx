---
"@openmapx/core": minor
---

Add the `overview` navigation camera mode, which releases the follow camera
without keeping the follow-puck offset, and circular-statistics helpers
(`dominantGridBearing`, `nearestGridBearing`, `bearingDelta`) for aligning a
map to the local street grid.
