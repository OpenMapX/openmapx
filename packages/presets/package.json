{
  "name": "@openmapx/presets",
  "license": "Apache-2.0",
  "version": "1.0.0",
  "description": "OSM preset matcher, chip translations, and tag-set helpers built on iD's tagging schema for OpenMapX",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@openmapx/core": "workspace:^",
    "@openstreetmap/id-tagging-schema": "^6.18.0"
  },
  "devDependencies": {
    "@types/node": "^24.13.2",
    "typescript": "^7.0.2",
    "vitest": "^4.1.8"
  }
}
