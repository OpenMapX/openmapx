# Downloaded and compiled data (managed by manage.sh)
data/

# Traefik ACME certificates (auto-generated)
traefik/acme.json

# Traefik basic-auth credentials (per-server, use auth.yml.example as template)
traefik/dynamic/auth.yml

# Docker Compose environment (contains secrets)
.env
